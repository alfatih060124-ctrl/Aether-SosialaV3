services:
  - type: web
    name: aether-social-v3-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: EXECUTION_MODE
        value: SHADOW
      - key: LIVE_ENABLED
        value: "false"
      - key: FIXTURE_GATE_PASSED
        value: "false"
      - key: OPERATOR_APPROVED
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: aether-postgres
          property: connectionString
      - key: API_TOKEN
        sync: false

  - type: pserv
    name: aether-postgres
    env: docker
    plan: starter
    image:
      url: postgres:16-alpine
    envVars:
      - key: POSTGRES_DB
        value: aether
      - key: POSTGRES_USER
        value: aether
      - key: POSTGRES_PASSWORD
        sync: false
