name: Solana Block Time Corroboration Regression

on:
  pull_request:
    paths:
      - 'services/api/src/solana-block-time-corroboration.mjs'
      - 'scripts/solana-block-time-corroboration-regression.mjs'
      - '.github/workflows/solana-block-time-corroboration-regression.yml'
  push:
    branches: [main]
    paths:
      - 'services/api/src/solana-block-time-corroboration.mjs'
      - 'scripts/solana-block-time-corroboration-regression.mjs'
      - '.github/workflows/solana-block-time-corroboration-regression.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: node --check services/api/src/solana-block-time-corroboration.mjs && node --check scripts/solana-block-time-corroboration-regression.mjs
      - name: Run regression
        run: node scripts/solana-block-time-corroboration-regression.mjs
