name: Solana Token Balance Verifier Chronology Regression

on:
  pull_request:
    paths:
      - 'services/api/src/solana-token-balance-evidence-source.mjs'
      - 'scripts/solana-token-balance-verifier-chronology-regression.mjs'
      - '.github/workflows/solana-token-balance-verifier-chronology-regression.yml'
  push:
    branches: [main]
    paths:
      - 'services/api/src/solana-token-balance-evidence-source.mjs'
      - 'scripts/solana-token-balance-verifier-chronology-regression.mjs'
      - '.github/workflows/solana-token-balance-verifier-chronology-regression.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: node --check services/api/src/solana-token-balance-evidence-source.mjs && node --check scripts/solana-token-balance-verifier-chronology-regression.mjs
      - name: Run regression
        run: node scripts/solana-token-balance-verifier-chronology-regression.mjs
