name: Auto Trade Atomic Persistence Regression

on:
  pull_request:
    paths:
      - 'services/api/src/autotrade-atomic-persistence.mjs'
      - 'scripts/autotrade-atomic-persistence-regression.mjs'
      - '.github/workflows/autotrade-atomic-persistence.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/autotrade-atomic-persistence-regression.mjs
