name: Solana Lamport Balance Evidence Regression

on:
  pull_request:
    paths:
      - 'services/api/src/solana-lamport-balance-evidence.mjs'
      - 'scripts/regression-solana-lamport-balance-evidence.mjs'
      - '.github/workflows/solana-lamport-balance-evidence-regression.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node --check services/api/src/solana-lamport-balance-evidence.mjs
      - run: node --check scripts/regression-solana-lamport-balance-evidence.mjs
      - run: node scripts/regression-solana-lamport-balance-evidence.mjs
