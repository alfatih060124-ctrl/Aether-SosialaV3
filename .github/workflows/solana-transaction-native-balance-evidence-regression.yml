name: Solana Transaction Native Balance Evidence Regression

on:
  pull_request:
    paths:
      - 'services/api/src/solana-transaction-native-balance-evidence.mjs'
      - 'scripts/regression-solana-transaction-native-balance-evidence.mjs'
      - '.github/workflows/solana-transaction-native-balance-evidence-regression.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: node --check services/api/src/solana-transaction-native-balance-evidence.mjs && node --check scripts/regression-solana-transaction-native-balance-evidence.mjs
      - name: Run synthetic fail-closed regression
        run: node scripts/regression-solana-transaction-native-balance-evidence.mjs
