import { createHash } from 'node:crypto';

const B58='123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz';
const B58_SET=new Set(B58);
function fail(code){const e=new Error(code);e.code=code;throw e;}
function plain(v){return v!==null&&typeof v==='object'&&!Array.isArray(v);}
function stable(v){if(Array.isArray(v))return`[${v.map(stable).join(',')}]`;if(plain(v))return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${stable(v[k])}`).join(',')}}`;return JSON.stringify(v);}
function hash(v){return createHash('sha256').update(stable(v)).digest('hex');}
function b58len(text){if(typeof text!=='string'||!text.length)return-1;let n=0n;for(const ch of text){if(!B58_SET.has(ch))return-1;n=n*58n+BigInt(B58.indexOf(ch));}let bytes=0,x=n;while(x>0n){bytes++;x>>=8n;}let leading=0;while(leading<text.length&&text[leading]==='1')leading++;return bytes+leading;}
function solId(v,bytes,code){if(b58len(v)!==bytes)fail(code);return v;}
function safeInt(v,code){if(!Number.isSafeInteger(v)||v<0)fail(code);return v;}
function iso(v,code){if(typeof v!=='string')fail(code);const ms=Date.parse(v);if(!Number.isFinite(ms)||new Date(ms).toISOString()!==v)fail(code);return ms;}
function endpoint(v){if(typeof v!=='string'||!/^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$/.test(v))fail('invalid_solscan_source_label');return v;}
function normalizeStatus(v){if(v==='Success')return'SUCCESS';if(v==='Fail')return'FAIL';fail('invalid_transaction_status');}
function normalizePrograms(v){if(!Array.isArray(v))fail('invalid_program_ids');const seen=new Set();return v.map((x,i)=>{const id=solId(x,32,`invalid_program_id_${i}`);if(seen.has(id))fail('duplicate_program_id');seen.add(id);return id;});}
function normalizeSigners(v){if(!Array.isArray(v)||v.length===0)fail('invalid_signers');const seen=new Set();return v.map((x,i)=>{const id=solId(x,32,`invalid_signer_${i}`);if(seen.has(id))fail('duplicate_signer');seen.add(id);return id;});}
function normalizeRows(rows,wallet,observedMs){if(!Array.isArray(rows))fail('invalid_solscan_rows');const seen=new Set();let previousSlot=null;return rows.map((row,i)=>{if(!plain(row))fail(`invalid_solscan_row_${i}`);const signature=solId(row.tx_hash,64,`invalid_tx_hash_${i}`);if(seen.has(signature))fail('duplicate_tx_hash');seen.add(signature);const slot=safeInt(row.slot,`invalid_slot_${i}`);if(previousSlot!==null&&slot>previousSlot)fail('rows_not_descending');previousSlot=slot;const block_time=safeInt(row.block_time,`invalid_block_time_${i}`);if(block_time*1000>observedMs)fail('future_block_time');const fee_lamports=safeInt(row.fee,`invalid_fee_${i}`);const signers=normalizeSigners(row.signer);if(!signers.includes(wallet))fail('wallet_not_signer');const program_ids=normalizePrograms(row.program_ids??[]);return{signature,slot,block_time,fee_lamports,status:normalizeStatus(row.status),signers,program_ids,source_reference:`solscan:transaction:${signature}@${slot}`};});}
function envelope(p){const provenance={...p};return{schema:'aether.solscan.account_transaction_evidence.v1',collection_status:'PENDING_DATA',metrics_available:false,trades_count:null,total_return_bps:null,win_rate_bps:null,drawdown_bps:null,reputation_score:null,verified:false,published:false,live_execution_authorized:false,reconciliation_required:true,source_reference:null,evidence_count:p.rows.length,rows:p.rows,provenance,source_hash:hash(provenance)};}

export async function collectSolscanAccountTransactionEvidence({query,traderWallet,sourceLabel='solscan_pro_v2',limit=20,before=null,requestedAt,observedAt}){
  if(typeof query!=='function')fail('solscan_query_required');const requested_wallet=solId(traderWallet,32,'invalid_trader_wallet');const source_label=endpoint(sourceLabel);if(![10,20,30,40].includes(limit))fail('invalid_limit');const before_signature=before===null?null:solId(before,64,'invalid_before_signature');const req=iso(requestedAt,'invalid_requested_at'),obs=iso(observedAt,'invalid_observed_at');if(obs<req)fail('invalid_observation_chronology');
  const response=await query({path:'/v2.0/account/transactions',address:requested_wallet,limit,before:before_signature});if(!plain(response)||response.success!==true||!Array.isArray(response.data))fail('invalid_solscan_response');const rows=normalizeRows(response.data,requested_wallet,obs);if(rows.length>limit)fail('result_exceeds_limit');
  return envelope({provider:'SOLSCAN_PRO_V2',source_label,endpoint_path:'/v2.0/account/transactions',requested_wallet,limit,before_signature,requested_at:requestedAt,observed_at:observedAt,rows});
}

export function verifySolscanAccountTransactionEvidence(e){
  if(!plain(e)||e.schema!=='aether.solscan.account_transaction_evidence.v1')return false;
  try{if(e.collection_status!=='PENDING_DATA'||e.metrics_available!==false||e.verified!==false||e.published!==false||e.live_execution_authorized!==false||e.reconciliation_required!==true||e.source_reference!==null)return false;for(const k of['trades_count','total_return_bps','win_rate_bps','drawdown_bps','reputation_score'])if(e[k]!==null)return false;const p=e.provenance;if(!plain(p)||p.provider!=='SOLSCAN_PRO_V2'||p.endpoint_path!=='/v2.0/account/transactions')return false;solId(p.requested_wallet,32,'bad_wallet');endpoint(p.source_label);if(![10,20,30,40].includes(p.limit))return false;if(p.before_signature!==null)solId(p.before_signature,64,'bad_before');const req=iso(p.requested_at,'bad_req'),obs=iso(p.observed_at,'bad_obs');if(obs<req)return false;const rpcRows=p.rows.map(r=>({tx_hash:r.signature,slot:r.slot,block_time:r.block_time,fee:r.fee_lamports,status:r.status==='SUCCESS'?'Success':'Fail',signer:r.signers,program_ids:r.program_ids}));const rows=normalizeRows(rpcRows,p.requested_wallet,obs);if(rows.length>p.limit||stable(rows)!==stable(p.rows))return false;if(e.evidence_count!==rows.length||stable(e.rows)!==stable(rows))return false;return e.source_hash===hash(p);}catch{return false;}
}
